/**
 * Tunable reel animation parameters (placeholder symbols unchanged).
 * Extracted from legacy slot demo constants — not gambling logic.
 */
export const REEL_SYMBOL_HEIGHT = 150;
export const REEL_SCROLL_DURATION_MS = 100;
/** Repeat counts per column (visual stagger). */
export const REEL_REPEAT_BY_COLUMN = [5, 10, 15, 20, 25] as const;
